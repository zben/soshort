!!!
%html{:xmlns => "http://www.w3.org/1999/xhtml"}
  %head
    %title= h(yield(:title) || "So Short!")
    = stylesheet_link_tag 'application','autoSuggest','bootstrap','custom'
    = javascript_include_tag 'jquery','jquery.autoSuggest','application'
    = yield(:head)
  %body
    .container.main
      - flash.each do |name, msg|
        = content_tag :div, msg, :id => "flash_#{name}"
      - if show_title?
        %h1= h yield(:title)
      = yield
